{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://akalynth/codex/council-proposal.schema.json",
  "title": "Akalynth Council Proposal (local ops) v1",
  "description": "Local Witness Council proposal. Governs ops action classes only; never auto-mutates runtime.",
  "type": "object",
  "required": [
    "schema_version",
    "object_id",
    "status",
    "title",
    "summary",
    "proposer_id",
    "created_utc",
    "action_class",
    "action_params",
    "packet_authority",
    "non_claims"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_version": { "const": "council-proposal/v1" },
    "object_id": {
      "type": "string",
      "pattern": "^AKALYNTH_COUNCIL_PROPOSAL_[A-Z0-9_]+$",
      "description": "Stable proposal identifier."
    },
    "status": {
      "enum": ["draft", "open", "voting", "approved", "rejected", "executed", "expired", "blocked"],
      "description": "Machine source-of-truth for council workflow state."
    },
    "title": { "type": "string", "minLength": 1 },
    "summary": { "type": "string", "minLength": 1 },
    "proposer_id": {
      "type": "string",
      "description": "Council member id (operator handle, agent id, or local member slug)."
    },
    "created_utc": { "type": "string", "format": "date-time" },
    "voting_opens_utc": { "type": "string", "format": "date-time" },
    "voting_closes_utc": { "type": "string", "format": "date-time" },
    "action_class": {
      "enum": [
        "lane:beta:check",
        "lane:staging:check",
        "lane:sim:check",
        "lane:beta:deploy",
        "lane:staging:deploy"
      ],
      "description": "v1 check classes are read-only. Deploy classes require DEPLOY_PERMIT_V1 + human ack."
    },
    "action_params": {
      "type": "object",
      "additionalProperties": false,
      "required": ["lane"],
      "properties": {
        "lane": { "enum": ["beta", "staging", "sim"] },
        "dry_run": { "type": "boolean", "default": false },
        "execution_ack_required": {
          "type": "boolean",
          "default": false,
          "description": "Deploy classes must set true; gate refuses deploy without human ack."
        }
      }
    },
    "quorum": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "min_approvals": { "type": "integer", "minimum": 1, "default": 1 },
        "min_participation": { "type": "integer", "minimum": 1, "default": 1 },
        "min_weighted_approvals": {
          "type": "number",
          "minimum": 0,
          "description": "v2 optional weighted quorum floor (registry-resolved vote weights)."
        }
      }
    },
    "packet_authority": {
      "type": "object",
      "additionalProperties": false,
      "required": ["object_id", "path"],
      "properties": {
        "object_id": {
          "enum": [
            "AKALYNTH_COUNCIL_DAO_V1",
            "AKALYNTH_COUNCIL_DAO_V2",
            "AKALYNTH_COUNCIL_DAO_DEPLOY_PERMIT_V1"
          ]
        },
        "path": { "type": "string" }
      }
    },
    "source_inputs": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Repo-relative paths or ops artifacts informing the proposal."
    },
    "codex_entry_id": {
      "type": "string",
      "const": "council-dao",
      "description": "Link to akalynth-codex live object."
    },
    "non_claims": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1
    },
    "prior_receipt_hash": {
      "type": ["string", "null"],
      "description": "Optional hash chaining to prior council receipt."
    }
  }
}