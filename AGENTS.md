# akalynth-codex

World graph source authority: codex entries, schema, projection tools, builder/operator/agent surfaces.

- Not the public website (`akalynth-site`) and not the game runtime (`akalynth`).
- Publication is review-gated via `tools/project.mjs` and `tools/build-site.mjs`.
- Custody path: `akalynth-ops/repos/akalynth-codex` (symlinked from `akalynth-ops/codex`).
